package org.yetyman.controls.treeview;

public enum AlignmentMethod {
    //faster, simpler, less error-prone
    INDEX,
    //slower for longer distances from the visible cell, but more natural
    LENGTH;
}
